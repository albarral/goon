
LOGGING CONFIGURATION

log4cxx configuration files must be located in a .tron folder in user's HOME directory.

Steps to follow:
- create a .tron folder in the home directory
- copy all xml files from this logging directory to that folder.

> 
cd 
mkdir .tron
cp /path_to_goon/logging/*.xml .tron
